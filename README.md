$
$ export FLASK_APP=run.pyexport FLASK_CONFIG=development export FLASK_APP=run.py
$ flask run

$ flask db init
$ flask db migrate
$ flask db upgrade

export FLASK_APP=run.pyexport FLASK_CONFIG=development export FLASK_APP=run.py